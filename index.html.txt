<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>MotivatedScholar</title>
  <meta name="description" content="Motivation, study tips, student-life improvement — MotivatedScholar (Hindi + English)">
  <meta name="keywords" content="study tips, motivation, student life, productivity, Hindi, English">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500&family=Poppins:wght@600&display=swap" rel="stylesheet">
 <meta name="google-site-verification" content="googlecb63da56eeba24c2" />
  <style>
    :root{--bg:#ffffff;--card:#fff;--muted:#6b7280;--accent:#0b6efd;--highlight:#10b981;--maxw:1100px}
    [data-theme="dark"]{--bg:#0b1220;--card:#071022;--muted:#94a3b8;--accent:#4f8cff;--highlight:#22c55e}
    *{box-sizing:border-box}
    body{margin:0;font-family:Inter,system-ui,-apple-system,'Segoe UI',Roboto,'Helvetica Neue',Arial;color:var(--muted);background:var(--bg);-webkit-font-smoothing:antialiased}
    .wrap{max-width:var(--maxw);margin:28px auto;padding:20px}
    header{display:flex;align-items:center;justify-content:space-between;gap:12px}
    .brand{display:flex;align-items:center;gap:12px}
    .logo{width:56px;height:56px;border-radius:12px;background:linear-gradient(135deg,var(--accent),#7c3aed);display:flex;align-items:center;justify-content:center;color:white;font-weight:700;font-size:18px}
    h1{margin:0;font-size:1.25rem;color:inherit;font-family:Poppins, sans-serif}
    .controls{display:flex;gap:8px;align-items:center}
    .btn{padding:8px 12px;border-radius:10px;border:1px solid transparent;background:transparent;color:inherit;cursor:pointer}
    .btn.primary{background:var(--accent);color:white;border-color:rgba(0,0,0,0.05)}

    .hero{display:grid;grid-template-columns:1fr 320px;gap:20px;margin-top:18px;align-items:center}
    .hero-card{padding:18px;border-radius:14px;background:var(--card);box-shadow:0 6px 18px rgba(2,6,23,0.06)}
    .hero h2{margin:0 0 8px 0;color:inherit;font-family:Poppins, sans-serif}
    .hero p{margin:0 0 12px 0;color:var(--muted)}
    .search{display:flex;gap:8px;align-items:center}
    .search input{padding:10px 12px;border-radius:10px;border:1px solid #e6eef8;min-width:220px}

    .grid{display:grid;grid-template-columns:1fr;gap:18px;margin-top:18px}
    @media(min-width:880px){.grid{grid-template-columns:2fr 380px}}
    .posts{display:grid;gap:14px}
    .post{background:var(--card);padding:14px;border-radius:12px;border:1px solid rgba(0,0,0,0.03)}
    .post h3{margin:0 0 6px 0;color:inherit;font-family:Poppins, sans-serif}
    .meta{font-size:0.82rem;color:var(--muted);margin-bottom:8px}
    .excerpt{color:var(--muted)}
    .tag{display:inline-block;background:rgba(11,110,253,0.09);color:var(--accent);padding:5px 8px;border-radius:999px;font-size:0.78rem;margin-right:6px}

    .sidebar .card{padding:12px;border-radius:12px;background:var(--card)}
    .toggle{display:flex;gap:6px;align-items:center}
    footer{margin-top:22px;color:var(--muted);font-size:0.9rem}
    a{color:var(--accent);text-decoration:none}

    .lang {padding:6px 10px;border-radius:8px;border:1px solid rgba(0,0,0,0.05);cursor:pointer}
    .small{font-size:0.9rem}
    .center{display:flex;align-items:center;gap:8px}
    input:focus, button:focus{outline:3px solid rgba(11,110,253,0.12);outline-offset:2px}
  </style>
</head>
<body>
  <div class="wrap" id="app">
    <header>
      <div class="brand">
        <div class="logo">MS</div>
        <div>
          <h1>MotivatedScholar</h1>
          <div class="small">Ignite Your Mind, Elevate Your Life — हर दिन कुछ बेहतर सीखो!</div>
        </div>
      </div>
      <div class="controls">
        <div class="center">
          <button class="btn" id="themeToggle" aria-label="Toggle theme">🌙</button>
          <div class="lang center" id="langToggle">हिन्दी / EN</div>
        </div>
      </div>
    </header>

    <section class="hero">
      <div class="hero-card">
        <h2>Welcome, Scholar!</h2>
        <p>MotivatedScholar par tum paoge daily motivation, study strategies, time-management tips, aur real student-life stories — Hindi & English dono mein.</p>
        <div class="search">
          <input id="searchInput" placeholder="Search posts, tips, topics..." aria-label="Search posts">
          <button class="btn" id="clearSearch">Clear</button>
        </div>
        <div style="margin-top:12px">
          <span class="tag">Study</span>
          <span class="tag">Motivation</span>
          <span class="tag">Student-Life</span>
        </div>
      </div>

      <div class="hero-card" aria-hidden="false">
        <svg viewBox="0 0 200 160" width="100%" height="160" preserveAspectRatio="xMidYMid meet">
          <defs><linearGradient id="g1" x1="0" x2="1"><stop offset="0" stop-color="#0b6efd"/><stop offset="1" stop-color="#7c3aed"/></linearGradient></defs>
          <rect x="8" y="20" width="184" height="120" rx="12" fill="url(#g1)" opacity="0.12"/>
          <g transform="translate(18,30)"><rect x="0" y="0" width="90" height="12" rx="6" fill="#fff" opacity="0.9"></rect><rect x="0" y="22" width="130" height="10" rx="6" fill="#fff" opacity="0.7"></rect><circle cx="140" cy="45" r="28" fill="#fff" opacity="0.9"></circle><text x="136" y="50" font-size="10" text-anchor="middle" fill="#0b6efd" font-weight="600">MS</text></g>
        </svg>
      </div>
    </section>

    <div class="grid">
      <main class="posts" id="posts" aria-live="polite">
        <article class="post" data-title="Rise early, study better" data-lang="en" data-tags="study,motivation">
          <div class="meta"><strong>Motivation</strong> • 30 Oct 2025</div>
          <h3>Rise early, study better</h3>
          <p class="excerpt">Small changes in morning routine can boost focus. Start with 20 minutes of review before classes. <a href="#" onclick="return false;">Read more →</a></p>
        </article>
        <article class="post" data-title="Parhai ke 5 zabardast tips" data-lang="hi" data-tags="study,tips">
          <div class="meta"><strong>Study Tips</strong> • 25 Oct 2025</div>
          <h3>Parhai ke 5 zabardast tips</h3>
          <p class="excerpt">Pomodoro use karo, active recall practice karo, aur regular breaks lo. Yeh chhote steps bahut farq laate hain. <a href="#" onclick="return false;">Read more →</a></p>
        </article>
        <article class="post" data-title="How to manage exam stress" data-lang="en" data-tags="student-life,wellbeing">
          <div class="meta"><strong>Student Life</strong> • 20 Oct 2025</div>
          <h3>How to manage exam stress</h3>
          <p class="excerpt">Simple breathing exercises, scheduled revision, and small rewards can reduce anxiety. Try box breathing for 5 minutes. <a href="#" onclick="return false;">Read more →</a></p>
        </article>
      </main>

      <aside class="sidebar">
        <div class="card" style="margin-bottom:12px">
          <h4>Filters</h4>
          <div style="margin-top:8px">
            <label><input type="checkbox" class="filter" value="study" checked> Study</label><br>
            <label><input type="checkbox" class="filter" value="motivation" checked> Motivation</label><br>
            <label><input type="checkbox" class="filter" value="student-life" checked> Student Life</label>
          </div>
        </div>
        <div class="card" style="margin-bottom:12px">
          <h4>Language</h4>
          <div style="margin-top:8px">
            <label><input type="radio" name="lang" value="all" checked> All</label><br>
            <label><input type="radio" name="lang" value="en"> English</label><br>
            <label><input type="radio" name="lang" value="hi"> हिन्दी</label>
          </div>
        </div>
        <div class="card">
          <h4>About</h4>
          <p class="small">MotivatedScholar — daily practical tips for students. Own this site and host on GitHub Pages for free.</p>
        </div>
      </aside>
    </div>

    <footer>
      <h4>Deploy & SEO</h4>
      <p>Upload index.html to GitHub Pages, verify Google Search Console using meta tag, and use below SEO files.</p>
      <pre>&lt;?xml version="1.0" encoding="UTF-8"?&gt;
&lt;urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"&gt;
  &lt;url&gt;&lt;loc&gt;https://username.github.io/motivatedscholar/&lt;/loc&gt;&lt;/url&gt;
&lt;/urlset&gt;</pre>
      <pre>robots.txt:
User-agent: *
Allow: /
Sitemap: https://username.github.io/motivatedscholar/sitemap.xml</pre>
    </footer>
  </div>

  <script>
    (function(){
      const root=document.documentElement;
      const stored=localStorage.getItem('ms-theme');
      if(stored) root.setAttribute('data-theme',stored);
      document.getElementById('themeToggle').addEventListener('click',()=>{
        const next=root.getAttribute('data-theme')==='dark'?'':'dark';
        if(next) root.setAttribute('data-theme','dark'); else root.removeAttribute('data-theme');
        localStorage.setItem('ms-theme',next);
      });
      const posts=Array.from(document.querySelectorAll('.post'));
      const searchInput=document.getElementById('searchInput');
      const clearBtn=document.getElementById('clearSearch');
      const filters=Array.from(document.querySelectorAll('.filter'));
      const langRadios=document.querySelectorAll('input[name="lang"]');
      function applyFilters(){
        const q=(searchInput.value||'').toLowerCase().trim();
        const lang=document.querySelector('input[name="lang"]:checked').value;
        const activeTags=filters.filter(f=>f.checked).map(f=>f.value);
        posts.forEach(p=>{
          const title=p.dataset.title.toLowerCase();
          const tags=(p.dataset.tags||'').toLowerCase().split(',');
          const postLang=p.dataset.lang||'en';
          const matchesQuery=q===''||title.includes(q)||p.innerText.toLowerCase().includes(q);
          const matchesLang=lang==='all'||postLang===lang;
          const matchesTags=activeTags.length===0||tags.some(t=>activeTags.includes(t));
          p.style.display=(matchesQuery&&matchesLang&&matchesTags)?'':'none';
        });
      }
      searchInput.addEventListener('input',applyFilters);
      clearBtn.addEventListener('click',()=>{searchInput.value='';applyFilters();});
      filters.forEach(f=>f.addEventListener('change',applyFilters));
      langRadios.forEach(r=>r.addEventListener('change',applyFilters));
      applyFilters();
    })();
  </script>
</body>
</html>


